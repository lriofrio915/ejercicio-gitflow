import * as React from 'react';

// Delete me
export const Thing = () => {
  return <div>the snozzberries taste like snozzberries</div>;
};

const sumar = (num1: any, num2: any) => {
  return num1 + num2;
};

let res = sumar(5, 11);
console.log(res);
